# 错题本简易验证审核通道规则

适用项目：李兆霖数学错题本、李兆霖化学错题本、李兆霖物理错题本。

## 1. 定义

“简易验证”只免除每道题从零开始的完整独立重解，不免除逐题审核，也不是按批次直接把 `verified` 改为 `1`。

每道题仍必须生成 `audit-item` 审核包，形成一份结构化审核记录，并通过
`verify-item`（或逐项调用同一质量门的 `verify-review-batch`）提交。

## 2. 准入条件

一道题只有同时满足以下条件，才可进入简易验证通道：

1. **用户明确确认批次**：用户明确指定某个来源、导入批次或日期范围为高质量材料；不得仅凭学校名气、来源声誉、其他模型标记或抽样结果推定。
2. **来源与权限完整**：来源名称、文件路径或 URL、许可说明、获取时间和 `rights_confirmed` 均完整。
3. **结构完整**：
   - 题干完整；
   - 选择题选项完整；
   - 答案和逐步解析存在；
   - 图片、表格、装置图或实验图路径有效；
   - 无乱码、私用字符、断裂公式、占位答案或残留编辑标记。
4. **可逐题审计**：题目已进入唯一主库且保持 `verified=0`，可由正式
   `audit-item → verify-item` 流程处理。
5. **没有已知冲突**：导入预检没有发现题干、答案、解析、标签或来源之间的明确冲突。

未被用户明确列入可靠范围的题，默认走完整独立审核。

## 3. 简易通道仍必须逐题检查的项目

每题都要核对：

1. 题干、条件、选项、图表和附加材料完整；
2. 答案与解析结论一致；
3. 解析的关键逻辑、公式、分支和结论自洽；
4. 知识点、题型、难度、结构特征和推荐目标合理；
5. 来源、授权与原始载荷可追溯；
6. 精确重复和近似重复项已处理；
7. 学科专属质量门全部通过；
8. 审核记录中的 `checks_confirmed=true`，并明确记录一致性依据。

简易审核可以引用原解析中的关键依据，但不能只复制长解析，也不能只写
“来源可靠”“答案正确”或“抽查通过”。

## 4. 必须退出简易通道、恢复独立推导的条件

出现以下任一情况，当前题立即恢复完整独立推导：

- 题干、选项、公式、图表、装置或关键条件缺失、模糊或互相矛盾；
- 答案与解析不一致，或解析存在跳步、循环论证、分类遗漏、边界遗漏；
- 出现占位答案，例如“见解析”“略”“答案见后”；
- OCR、公式转换或图片提取存在疑点；
- 自动检查失败、结果未知，或形式化表达式与原题无法可靠对应；
- 近似重复题的条件或答案不同，无法直接确认是否为同题；
- 标签、难度或题型与实际内容明显不符；
- 来源、许可或原始记录不完整；
- 模型对答案或解析产生任何实质性疑问。

恢复独立推导后：

- `pass`：独立结论与原题一致；
- `corrected`：修正题干、答案、解析或标签后通过；
- `needs_revision`：证据不足或尚需补全，保持未验证；
- `reject`：错误、不可恢复或不应保留，保持未验证并记录原因。

## 5. 学科专属质量门

### 5.1 数学

- 公式、定义域、参数范围、分类讨论、充分必要性；
- 几何图形、辅助线、坐标与手写步骤；
- 证明题的逻辑方向和边界情况；
- SymPy 等程序只验证已形式化的表达式，不替代题意、图形和证明审核。

### 5.2 化学

- 元素符号、下标、电荷、系数、物态、箭头和反应条件；
- 元素守恒、电荷守恒、电子守恒、计量关系、单位和有效数字；
- 反应物/产物选择、平衡条件、氧化还原方向和有机反应选择性；
- 实验操作、现象、证据链、装置连接、安全与环保；
- 自动守恒检查只提供证据，不能替代产物、条件、装置和化学合理性判断。

### 5.3 物理

- 研究对象、物理过程、参考系、正方向和适用模型；
- 初末状态、边界/临界条件、约束关系和近似条件；
- 矢量方向、正负号、单位、量纲、有效数字；
- 受力图、电路图、光路图、实验装置和函数图像；
- 自动量纲检查只提供证据，不能证明模型选择、方向、边界和答案正确。

## 6. 批量流程

```powershell
python -B <skill-dir>\scripts\notebook.py doctor --json
python -B <skill-dir>\scripts\notebook.py agent-context --task verify --json
python -B <skill-dir>\scripts\notebook.py audit-summary --json
python -B <skill-dir>\scripts\notebook.py audit-queue --simplified-only --limit 20 --json
python -B <skill-dir>\scripts\notebook.py prepare-audit-batch --simplified-only --limit 20 --out-dir <dir> --json
python -B <skill-dir>\scripts\notebook.py prepare-review-batch <decisions.json> --out-dir <dir> --json
python -B <skill-dir>\scripts\notebook.py verify-review-batch <dir>\manifest.json --json
```

批处理只压缩机械步骤和输出，不得减少逐题检查数量，不得使用批量 SQL 修改
`verified`。

## 7. 跨项目同步方案

三个项目应共享以上审核语义，但各自主库、Skill、审核记录和学科专属检查保持隔离。

安全启用步骤：

1. 在对应项目中记录用户确认的**具体来源/批次白名单**；
2. 仅白名单题返回 `verification_mode=simplified`；
3. 非白名单题始终返回 `verification_mode=full`；
4. 任何结构异常或审核疑点在单题层面强制恢复 `full`；
5. `audit-summary` 同时报告 `simplified_eligible` 与 `full_review_required`；
6. 为“未确认来源、已确认来源、异常回退、逐题提交”增加回归测试；
7. 启用后运行 `doctor`、完整测试和 `handoff`。

不建议仅设置一个全局起始日期后放行该日期之后的全部来源；化学、物理应优先使用
来源/批次白名单，日期只能作为用户明确确认范围的一部分。

## 8. 当前项目状态（2026-07-28）

| 项目 | 简易通道代码 | 当前策略 | 同步结论 |
|---|---|---|---|
| 数学错题本 | 已实现并启用 | `2026-07-19-g11-beijing-20` 及用户确认的 2026-07-20 起高质量试卷 | 正在使用 |
| 化学错题本 | 已实现但关闭 | 全部题完整独立审核 | 可同步；需先确认可靠来源/批次，并建议改为白名单 |
| 物理错题本 | 已实现但关闭 | 全部题完整独立审核 | 可同步；需先确认可靠来源/批次，并建议改为白名单 |

化学和物理项目不应直接继承数学项目的日期、批次或验证状态。
